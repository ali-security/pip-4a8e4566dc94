--use-feature=2020-resolver
cryptography==2.8
csv23
enum34; python_version < '3.4'
freezegun
mock
pretend
pytest
pytest-cov
pytest-rerunfailures
pytest-timeout
pytest-xdist
pyyaml
scripttest
setuptools>=39.2.0  # Needed for `setuptools.wheel.Wheel` support.
# pypa/virtualenv's `legacy` branch was deleted upstream, so the archive URL
# this line used to carry now 404s. `virtualenv<20` is the 16.7.x line that
# branch tracked, and is what pip's own test helpers expect.
virtualenv<20
werkzeug==0.16.0
wheel
